name: v0.1.5 inbound UI verification

on:
  push:
    branches: [fix/v0.1.5-inbound-ui]
    paths: [.github/workflows/v015-inbound-ui-verify.yml]

permissions:
  contents: write

jobs:
  fix-and-verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: fix/v0.1.5-inbound-ui
          fetch-depth: 0

      - name: Apply v0.1.5 fixes
        shell: python
        run: |
          from pathlib import Path
          import re

          # Randomized automatic port selection, while still scanning the whole configured range.
          Path('internal/service/ports.go').write_text(r'''package service

import (
    cryptorand "crypto/rand"
    "fmt"
    "math/big"
    "net"
    "strconv"

    "github.com/Kerberos255/X-port/internal/model"
)

func ensurePortFree(port int) error {
    addr := net.JoinHostPort("", strconv.Itoa(port))
    tcp, err := net.Listen("tcp", addr)
    if err != nil { return fmt.Errorf("port %d is already in use (TCP)", port) }
    _ = tcp.Close()
    udp, err := net.ListenPacket("udp", addr)
    if err != nil { return fmt.Errorf("port %d is already in use (UDP)", port) }
    _ = udp.Close()
    return nil
}

func randomPortStart(minPort, maxPort int) int {
    count := maxPort - minPort + 1
    if count <= 1 { return minPort }
    n, err := cryptorand.Int(cryptorand.Reader, big.NewInt(int64(count)))
    if err != nil { return minPort }
    return minPort + int(n.Int64())
}

func nextFreePortFrom(accounts []model.Account, minPort, maxPort, reserved, start int) int {
    if minPort < 1 || maxPort < minPort { return 0 }
    if start < minPort || start > maxPort { start = minPort }
    used := map[int]bool{reserved: true}
    for _, a := range accounts { used[a.Port] = true }
    count := maxPort - minPort + 1
    for i := 0; i < count; i++ {
        p := minPort + ((start - minPort + i) % count)
        if used[p] { continue }
        if ensurePortFree(p) == nil { return p }
    }
    return 0
}

func nextFreePort(accounts []model.Account, minPort, maxPort, reserved int) int {
    return nextFreePortFrom(accounts, minPort, maxPort, reserved, randomPortStart(minPort, maxPort))
}
''')

          Path('internal/service/port_suggestion.go').write_text(r'''package service

import "fmt"

func (s *Accounts) SuggestPort() (port, minPort, maxPort int, err error) {
    s.mu.Lock()
    defer s.mu.Unlock()
    accounts, err := s.store.Accounts()
    if err != nil { return 0, 0, 0, err }
    minPort, maxPort, apiPort := s.portDefaults()
    port = nextFreePort(accounts, minPort, maxPort, apiPort)
    if port == 0 { return 0, minPort, maxPort, fmt.Errorf("no automatic port available in %d-%d", minPort, maxPort) }
    return port, minPort, maxPort, nil
}
''')

          Path('internal/server/port_suggestion.go').write_text(r'''package server

import "net/http"

func (s *Server) suggestAccountPort(w http.ResponseWriter, r *http.Request) {
    port, minPort, maxPort, err := s.accounts.SuggestPort()
    if err != nil { writeError(w, 409, err.Error()); return }
    writeJSON(w, 200, map[string]int{"port": port, "min": minPort, "max": maxPort})
}
''')

          p=Path('internal/server/ux_routes.go'); s=p.read_text()
          needle='mux.Handle("GET /api/accounts/online", s.require(http.HandlerFunc(s.accountOnlineConnections)))'
          assert needle in s
          s=s.replace(needle, needle+'\n\tmux.Handle("GET /api/accounts/port-suggestion", s.require(http.HandlerFunc(s.suggestAccountPort)))', 1)
          p.write_text(s)

          # Built-in VLESS editor does not expose VLESS Encryption, so Vision is valid only on RAW/TCP + TLS/REALITY.
          p=Path('internal/accountcfg/account.go'); s=p.read_text()
          old='''\tcase "vless":
\t\tif in.Credential == "" {
\t\t\tin.Credential = newUUID()
\t\t}
\t\tif in.Flow == "" {
\t\t\tin.Flow = "xtls-rprx-vision"
\t\t}
\t\tif in.Security == "" {
\t\t\tin.Security = "reality"
\t\t}'''
          new='''\tcase "vless":
\t\tif in.Credential == "" {
\t\t\tin.Credential = newUUID()
\t\t}
\t\tif in.Security == "" {
\t\t\tin.Security = "reality"
\t\t}
\t\tif in.Flow == "" && (in.Network == "tcp" || in.Network == "raw") && (in.Security == "tls" || in.Security == "reality") {
\t\t\tin.Flow = "xtls-rprx-vision"
\t\t}'''
          assert old in s
          s=s.replace(old,new,1)

          # Reject combinations that current Xray transport compatibility does not support.
          marker='''\tif in.Protocol == "vmess" && in.ClientSecurity != "" {'''
          assert marker in s
          checks='''\tif in.Security == "reality" {
\t\tswitch in.Network {
\t\tcase "tcp", "raw", "xhttp", "grpc":
\t\tdefault:
\t\t\treturn fmt.Errorf("REALITY is not supported with %s transport", in.Network)
\t\t}
\t}
\tif in.Protocol == "vless" && in.Flow != "" {
\t\tif in.Flow != "xtls-rprx-vision" {
\t\t\treturn fmt.Errorf("unsupported VLESS flow %q", in.Flow)
\t\t}
\t\tif (in.Network != "tcp" && in.Network != "raw") || (in.Security != "tls" && in.Security != "reality") {
\t\t\treturn errors.New("xtls-rprx-vision requires RAW/TCP with TLS or REALITY in the built-in editor")
\t\t}
\t}
'''
          s=s.replace(marker,checks+marker,1)

          # Imported VLESS Encryption configs are preserved as read-only until that feature has a dedicated editor.
          marker='''\tcase "vless", "vmess", "trojan":
\t\tclient, err := oneObject(settings, "clients")'''
          assert marker in s
          repl='''\tcase "vless", "vmess", "trojan":
\t\tif v.Protocol == "vless" {
\t\t\tdecryption := strings.TrimSpace(stringValue(settings["decryption"]))
\t\t\tif decryption != "" && decryption != "none" { return false }
\t\t}
\t\tclient, err := oneObject(settings, "clients")'''
          s=s.replace(marker,repl,1)
          p.write_text(s)

          # XHTTP can store effective advanced values inside xhttpSettings.extra. Read/write that object when present.
          p=Path('internal/service/expert.go'); s=p.read_text()
          pattern=r'''\tif method == "xhttp" \{\n\t\tkey := expertXHTTPKey\(stream\).*?\n\t\tstream\[key\] = x\n\t\}\n\n\tsettingsJSON'''
          replacement='''\tif method == "xhttp" {
\t\tkey := expertXHTTPKey(stream)
\t\tx := expertObject(stream, key)
\t\ttarget := x
\t\tusesExtra := false
\t\tif extra, ok := x["extra"].(map[string]any); ok && extra != nil {
\t\t\ttarget, usesExtra = extra, true
\t\t}
\t\tmode := strings.TrimSpace(in.XHTTPMode)
\t\tswitch mode {
\t\tcase "", "auto", "packet-up", "stream-up", "stream-one":
\t\tdefault:
\t\t\treturn ExpertConfig{}, fmt.Errorf("unsupported XHTTP mode %q", mode)
\t\t}
\t\tsetExpertString(x, "mode", mode)
\t\tsetExpertString(target, "xPaddingBytes", in.XHTTPXPaddingBytes)
\t\tif in.XHTTPNoSSEHeader { target["noSSEHeader"] = true } else { delete(target, "noSSEHeader") }
\t\tif in.XHTTPScMaxBufferedPosts > 0 { target["scMaxBufferedPosts"] = in.XHTTPScMaxBufferedPosts } else { delete(target, "scMaxBufferedPosts") }
\t\tsetExpertString(target, "scMaxEachPostBytes", in.XHTTPScMaxEachPostBytes)
\t\tsetExpertString(target, "scStreamUpServerSecs", in.XHTTPScStreamUpServerSecs)
\t\tsetExpertString(target, "uplinkHTTPMethod", strings.ToUpper(strings.TrimSpace(in.XHTTPUplinkHTTPMethod)))
\t\tif usesExtra { x["extra"] = target }
\t\tstream[key] = x
\t}

\tsettingsJSON'''
          s2,n=re.subn(pattern,replacement,s,count=1,flags=re.S)
          assert n==1, n
          s=s2
          pattern=r'''\tif out\.SupportsXHTTP \{\n\t\tx := expertObject\(stream, expertXHTTPKey\(stream\)\).*?\n\t\}\n\treturn out, nil'''
          replacement='''\tif out.SupportsXHTTP {
\t\tx := expertObject(stream, expertXHTTPKey(stream))
\t\ttarget := x
\t\tif extra, ok := x["extra"].(map[string]any); ok && extra != nil { target = extra }
\t\tout.XHTTPMode = expertString(x["mode"])
\t\tout.XHTTPXPaddingBytes = expertScalarString(target["xPaddingBytes"])
\t\tout.XHTTPNoSSEHeader = expertBool(target["noSSEHeader"])
\t\tout.XHTTPScMaxBufferedPosts = expertInt64(target["scMaxBufferedPosts"])
\t\tout.XHTTPScMaxEachPostBytes = expertScalarString(target["scMaxEachPostBytes"])
\t\tout.XHTTPScStreamUpServerSecs = expertScalarString(target["scStreamUpServerSecs"])
\t\tout.XHTTPUplinkHTTPMethod = expertString(target["uplinkHTTPMethod"])
\t}
\treturn out, nil'''
          s2,n=re.subn(pattern,replacement,s,count=1,flags=re.S)
          assert n==1,n
          p.write_text(s2)

          # System form: a port field, aligned with Base Path; current-password confirmation appears only when changing admin credentials.
          p=Path('web/dist/index.html'); s=p.read_text()
          s=s.replace('<link rel="stylesheet" href="/v5.css?v=0.1.4">','<link rel="stylesheet" href="/v5.css?v=0.1.4"><link rel="stylesheet" href="/v6.css?v=0.1.5">')
          s=s.replace('<label>面板监听<input name="panelListen" placeholder="127.0.0.1:8080"></label>','<label>面板端口<input name="panelListen" inputmode="numeric" pattern="[0-9]*" placeholder="例如 50010"></label>')
          s=s.replace('<label class="full">当前密码（修改管理员时需要）<input name="currentPassword" type="password"></label>','')
          s=s.replace('面板监听、Base Path 或 HTTPS 变更后需要重启 X-port；防火墙仍由你手动放行。','面板端口、Base Path 或 HTTPS 变更后需要重启 X-port；防火墙仍由你手动放行。')
          s=s.replace('<script src="/v5-runtime.js?v=0.1.4" defer></script>','<script src="/v5-runtime.js?v=0.1.4" defer></script><script src="/v6.js?v=0.1.5" defer></script>')
          p.write_text(s)

          Path('web/dist/v6.css').write_text(r'''/* X-port v0.1.5: inbound editor and settings layout corrections. */
.modal-card{overflow-x:hidden}
.account-editor-footer{width:100%!important;max-width:100%!important;position:sticky!important;left:auto!important;right:auto!important;bottom:0!important;margin:18px 0 0!important;padding:15px 0 2px!important;border-top:1px solid var(--line)!important;background:linear-gradient(180deg,rgba(13,19,31,.97),#0d131f 35%)!important;box-shadow:0 -10px 20px rgba(5,9,15,.12)!important}
html[data-theme="light"] .account-editor-footer{background:linear-gradient(180deg,rgba(248,250,252,.98),#f8fafc 35%)!important;box-shadow:0 -10px 20px rgba(50,70,95,.06)!important}
.settings-form>label{align-self:start}.settings-form>label input,.settings-form>label select{min-height:42px}.settings-form .listen-hint{display:none!important}
.admin-reauth{grid-column:1/-1!important}.admin-reauth input{border-color:rgba(233,191,112,.34)!important}
.vless-flow.hidden-field{display:none!important}
.account-port-note{display:block;margin-top:3px;font-size:8px;color:#667990}
@media(max-width:700px){.account-editor-footer{margin:16px 0 0!important;padding:14px 0 calc(2px + env(safe-area-inset-bottom))!important}.editor-actions{margin-left:0}}
''')

          Path('web/dist/v6.js').write_text(r'''/* X-port v0.1.5: inbound correctness and system/settings UX. */
let xportPanelListenHostV6=''
let xportSettingsAdminV6=''
let xportPortRangeV6={min:20000,max:60000}

function parsePanelListenV6(value){
 const v=String(value||'').trim()
 if(/^\d+$/.test(v))return{host:'',port:v}
 let m=v.match(/^:(\d+)$/);if(m)return{host:'',port:m[1]}
 m=v.match(/^(\[[^\]]+\]|[^:]+):(\d+)$/);if(m)return{host:m[1],port:m[2]}
 return{host:'',port:v.replace(/^:/,'')}
}
function joinPanelListenV6(host,port){return host?`${host}:${port}`:`:${port}`}
function visionCompatibleV6(protocol,network,security){return protocol==='vless'&&['tcp','raw'].includes(network)&&['tls','reality'].includes(security)}
function securityChoicesV6(protocol,network){
 if(!['vless','trojan'].includes(protocol))return['none','tls']
 return['ws','websocket','httpupgrade'].includes(network)?['none','tls']:['none','tls','reality']
}
function normalizeTransportLabelV6(v){return({tcp:'RAW',raw:'RAW',xhttp:'XHTTP',grpc:'gRPC',ws:'WebSocket',websocket:'WebSocket',httpupgrade:'HTTPUpgrade'})[v]||v}

function protocolFieldsV6(p,a){
 const transport=!['socks','http'].includes(p),network=a.network||'tcp',security=a.security||(p==='vless'||p==='trojan'?'reality':'none');let cred=''
 if(p==='vless'){
  const canVision=visionCompatibleV6(p,network,security),flow=canVision?(a.flow||(a.id?'':'xtls-rprx-vision')):''
  cred=`<label>UUID<input name="credential" value="${esc(a.credential||'')}" placeholder="留空自动生成"></label><label class="vless-flow ${canVision?'':'hidden-field'}">Flow<select name="flow"><option value="" ${flow===''?'selected':''}>无</option><option value="xtls-rprx-vision" ${flow==='xtls-rprx-vision'?'selected':''}>xtls-rprx-vision</option></select></label>`
 }
 if(p==='vmess')cred=`<label>UUID<input name="credential" value="${esc(a.credential||'')}" placeholder="留空自动生成"></label><label>VMess Security<select name="clientSecurity">${['auto','aes-128-gcm','chacha20-poly1305','none','zero'].map(x=>option(x,x,a.clientSecurity||'auto')).join('')}</select></label>`
 if(p==='trojan')cred=`<label>Trojan 密码<input name="password" value="${esc(a.password||'')}" placeholder="留空自动生成"></label>`
 if(p==='shadowsocks')cred=`<label>加密方式<select name="method">${['chacha20-ietf-poly1305','aes-256-gcm','2022-blake3-aes-256-gcm','2022-blake3-chacha20-poly1305'].map(x=>option(x,x,a.method||'chacha20-ietf-poly1305')).join('')}</select></label><label>服务器密码<input name="serverPassword" value="${esc(a.serverPassword||'')}" placeholder="留空自动生成"></label><label>客户端密码<input name="password" value="${esc(a.password||'')}" placeholder="留空自动生成"></label>`
 if(p==='socks'||p==='http')cred=`<label>用户名<input name="username" value="${esc(a.username||'')}"></label><label>密码<input name="password" value="${esc(a.password||'')}" placeholder="留空自动生成"></label>`
 if(!transport)return `<div class="advanced-grid">${cred}</div>`
 const secChoices=securityChoicesV6(p,network),safeSecurity=secChoices.includes(security)?security:secChoices[0]
 return `<div class="advanced-grid">${cred}<label>传输<select name="network" id="network-select">${[['tcp','RAW'],['xhttp','XHTTP'],['grpc','gRPC'],['ws','WebSocket'],['httpupgrade','HTTPUpgrade']].map(([v,l])=>option(v,l,network)).join('')}</select></label><label>传输安全<select name="security" id="security-select">${secChoices.map(x=>option(x,x.toUpperCase(),safeSecurity)).join('')}</select></label><label class="transport-host">Host<input name="host" value="${esc(a.host||'')}"></label><label class="transport-path">Path<input name="path" value="${esc(a.path||'')}" placeholder="/"></label><label class="transport-service">gRPC Service<input name="serviceName" value="${esc(a.serviceName||'')}"></label></div>`
}
protocolFieldsV3=protocolFieldsV6

function syntheticExpertV6(protocol,network,security){
 const method=network==='tcp'?'raw':network
 return {protocol,method,security,acceptProxyProtocol:false,fallbacksJson:'[]',httpHeaderJson:'',supportsFallbacks:(protocol==='vless'||protocol==='trojan')&&method==='raw'&&security==='tls',supportsHttpHeader:method==='raw',supportsReality:security==='reality',supportsTls:security==='tls',supportsXhttp:method==='xhttp',xhttpMode:'auto',xhttpXPaddingBytes:'',xhttpNoSseHeader:false,xhttpScMaxBufferedPosts:0,xhttpScMaxEachPostBytes:'',xhttpScStreamUpServerSecs:'',xhttpUplinkHttpMethod:''}
}

refreshProtocolFieldsV3=function(full){
 const p=$('#protocol-select')?.value||full.protocol||'vless',box=$('#proto-fields');if(!box)return
 box.innerHTML=protocolFieldsV6(p,full)
 const net=$('#network-select'),sec=$('#security-select'),details=$('#account-advanced')
 const tune=()=>{
  const n=net?.value||'tcp',allowed=securityChoicesV6(p,n),old=sec?.value||'none'
  if(sec){const next=allowed.includes(old)?old:(allowed.includes('reality')?'reality':allowed[0]);sec.innerHTML=allowed.map(x=>option(x,x.toUpperCase(),next)).join('');sec.value=next}
  const security=sec?.value||'none'
  $$('.transport-host',box).forEach(e=>e.style.display=['ws','httpupgrade','xhttp'].includes(n)?'grid':'none')
  $$('.transport-path',box).forEach(e=>e.style.display=['ws','httpupgrade','xhttp'].includes(n)?'grid':'none')
  $$('.transport-service',box).forEach(e=>e.style.display=n==='grpc'?'grid':'none')
  const flow=box.querySelector('.vless-flow'),flowSelect=flow?.querySelector('select[name="flow"]'),canVision=visionCompatibleV6(p,n,security)
  if(flow){flow.classList.toggle('hidden-field',!canVision);if(!canVision&&flowSelect)flowSelect.value=''}
  if(!full.id&&details?.open){details.dataset.loaded='1';details._expertData=syntheticExpertV6(p,n,security);const c=$('#expert-content');if(c)c.innerHTML=renderExpertV3(details._expertData)}
 }
 net?.addEventListener('change',tune);sec?.addEventListener('change',tune);tune()
 if(!full.id&&details)details.addEventListener('toggle',()=>{if(details.open){const n=net?.value||'tcp',security=sec?.value||'none';details.dataset.loaded='1';details._expertData=syntheticExpertV6(p,n,security);const c=$('#expert-content');if(c)c.innerHTML=renderExpertV3(details._expertData)}})
}

function accountFormHTMLV6(a={}){
 const p=String(a.protocol||'vless').toLowerCase(),editing=Boolean(a.id)
 if(editing&&!a.editable)return `<div class="modal-title"><span>IMPORTED ACCOUNT</span><h2>${esc(a.name)}</h2></div><div class="readonly-note"><p>该账号包含当前内置编辑器不支持的配置，X-port 会保持原始 Xray 配置不变。</p></div>`
 const expiry=a.expiryTime?new Date(Number(a.expiryTime)-new Date().getTimezoneOffset()*60000).toISOString().slice(0,16):'',qgb=a.quotaBytes?(Number(a.quotaBytes)/1073741824).toFixed(2):'',enabled=editing?Boolean(a.enabled):true
 const range=a._portMin&&a._portMax?`${a._portMin}–${a._portMax}`:`${xportPortRangeV6.min}–${xportPortRangeV6.max}`
 return `<div class="modal-title"><span>${editing?'EDIT ACCOUNT':'NEW ACCOUNT'}</span><h2>${editing?'编辑账号':'新增账号'}</h2></div><form id="account-form" class="form-grid">
 <label class="protocol-first full">${reqLabelV3('协议')}<select name="protocol" id="protocol-select" ${editing?'disabled':''}>${protocols.map(x=>option(x,x.toUpperCase(),p)).join('')}</select>${editing?'<small class="field-hint">创建后不可修改</small>':''}</label>
 <label>${reqLabelV3('账号')}<input name="name" value="${esc(a.name||'')}" required maxlength="80"></label>
 <label>端口<input name="port" type="number" min="1" max="65535" value="${a.port||''}" placeholder="留空自动分配"><small class="account-port-note">自动端口范围 ${esc(range)}</small></label>
 <label>流量上限 / GB<input name="quota" type="number" min="0" step="0.1" value="${qgb}" placeholder="0 = 不限"></label>
 <label>到期时间<input name="expiry" type="datetime-local" value="${expiry}"></label>
 <div class="full proto-fields" id="proto-fields"></div>
 <details id="account-advanced" class="advanced account-advanced full"><summary><span>高级</span></summary><div id="expert-content" class="advanced-loading muted tiny"></div></details>
 <div class="account-editor-footer full"><div class="editor-policies">${editorPolicyV5('enabled','启用账号',enabled)}${editorPolicyV5('monthlyReset','每月 1 日自动清零',Boolean(a.monthlyReset))}</div><div class="editor-actions"><button type="button" class="btn ghost" id="cancel-form">取消</button><button type="submit" class="btn primary">${editing?'保存并应用':'创建并应用'}</button></div></div>
 </form>`
}
accountFormHTMLV3=accountFormHTMLV6

openAccountModalV3=async function(a=null){
 let full=a||{}
 if(a?.id){try{full=await api(`/api/accounts/${a.id}`)}catch(e){toast(e.message,true);return}}
 else{
  try{const d=await api('/api/accounts/port-suggestion');full={...full,port:d.port,_portMin:d.min,_portMax:d.max};xportPortRangeV6={min:d.min,max:d.max}}
  catch{try{const d=await api('/api/settings');xportPortRangeV6={min:Number(d.portMin)||20000,max:Number(d.portMax)||60000};full={...full,_portMin:xportPortRangeV6.min,_portMax:xportPortRangeV6.max}}catch{}}
 }
 openModal(accountFormHTMLV6(full));if(full.id&&!full.editable)return
 $('#cancel-form').onclick=closeModal
 refreshProtocolFieldsV3(full)
 const details=$('#account-advanced')
 if(full.id)details?.addEventListener('toggle',()=>{if(details.open&&!details.dataset.loaded){details.dataset.loaded='1';void loadExpertV3(full.id,details)}})
 $('#protocol-select')?.addEventListener('change',()=>{const p=$('#protocol-select').value;full={...full,protocol:p,credential:'',username:'',password:'',serverPassword:'',privateKey:'',shortId:'',serverName:'',dest:'',host:'',path:'',serviceName:'',network:'tcp',security:(p==='vless'||p==='trojan')?'reality':'none',flow:''};refreshProtocolFieldsV3(full)})
 $('#account-form').onsubmit=async e=>{
  e.preventDefault();const f=e.currentTarget,p=full.id?full.protocol:formValue(f,'protocol'),port=Number(formValue(f,'port')||0),fd=new FormData(f)
  const body={name:formValue(f,'name'),enabled:fd.get('enabled')==='on',monthlyReset:fd.get('monthlyReset')==='on',port,protocol:p,credential:formValue(f,'credential'),username:formValue(f,'username'),password:formValue(f,'password'),serverPassword:formValue(f,'serverPassword'),clientSecurity:formValue(f,'clientSecurity'),method:formValue(f,'method'),flow:formValue(f,'flow'),network:formValue(f,'network'),security:formValue(f,'security'),serverName:formValue(f,'serverName'),dest:formValue(f,'dest'),privateKey:formValue(f,'privateKey'),shortId:formValue(f,'shortId'),host:formValue(f,'host'),path:formValue(f,'path'),serviceName:formValue(f,'serviceName'),quotaBytes:Math.round(Number(formValue(f,'quota')||0)*1073741824),expiryTime:formValue(f,'expiry')?new Date(formValue(f,'expiry')).getTime():0}
  const expertData=(details?.dataset.loaded==='1'&&details._expertData)?expertBodyFromFormV3(details._expertData):null
  const b=f.querySelector('button[type=submit]');b.disabled=true;b.textContent='应用中…';const before=new Set(state.accounts.map(x=>Number(x.id)))
  try{
   const result=await mutationWithRecoveryV3(()=>full.id?api(`/api/accounts/${full.id}`,{method:'PUT',body:JSON.stringify(body)}):api('/api/accounts',{method:'POST',body:JSON.stringify(body)}),async()=>{if(full.id){const cur=await api(`/api/accounts/${full.id}`);return cur.name===body.name&&Number(cur.port)===(body.port||Number(cur.port))}const d=await api('/api/accounts');return(d.accounts||[]).some(x=>!before.has(Number(x.id))&&x.name===body.name)})
   let savedID=Number(full.id||result.value?.id||0)
   if(!savedID&&!full.id){const d=await api('/api/accounts');const created=(d.accounts||[]).find(x=>!before.has(Number(x.id))&&x.name===body.name);savedID=Number(created?.id||0)}
   if(expertData&&savedID)await mutationWithRecoveryV3(()=>api(`/api/accounts/${savedID}/expert`,{method:'PUT',body:JSON.stringify(expertData)}),async()=>Boolean(await api(`/api/accounts/${savedID}/expert`)))
   closeModal();await loadAccounts();await loadOverview();await loadOnlineConnectionsV3(false);toast(full.id?'账号已更新':'账号已创建')
  }catch(x){toast(x.message,true);b.disabled=false;b.textContent=full.id?'保存并应用':'创建并应用'}
 }
}
openAccountModal=openAccountModalV3

function settingsCredentialChangedV6(f){return String(formValue(f,'adminUsername')).trim()!==xportSettingsAdminV6||String(formValue(f,'newPassword')).length>0}
function syncAdminReauthV6(){
 const f=$('#settings-form');if(!f)return
 const changed=settingsCredentialChangedV6(f);let wrap=$('#settings-current-password-wrap')
 if(changed&&!wrap){const anchor=f.elements.namedItem('newPassword')?.closest('label');anchor?.insertAdjacentHTML('afterend','<label id="settings-current-password-wrap" class="admin-reauth">确认当前密码*<input name="currentPassword" type="password" autocomplete="current-password" required></label>')}
 if(!changed&&wrap)wrap.remove()
}
async function loadSettingsV6(){
 try{
  const d=await api('/api/settings'),f=$('#settings-form'),listen=parsePanelListenV6(d.panelListen)
  xportPanelListenHostV6=listen.host;xportSettingsAdminV6=String(d.adminUsername||'');xportPortRangeV6={min:Number(d.portMin)||20000,max:Number(d.portMax)||60000}
  for(const [k,v] of Object.entries(d)){if(k==='panelListen'||k==='currentPassword')continue;const el=f.elements.namedItem(k);if(el)el.value=v??''}
  f.elements.namedItem('panelListen').value=listen.port;f.elements.namedItem('newPassword').value='';$('#settings-current-password-wrap')?.remove()
 }catch(e){toast(e.message,true)}
}
async function saveSettingsV6(e){
 e.preventDefault();const f=e.currentTarget,port=String(formValue(f,'panelListen')||'').trim()
 if(!/^\d+$/.test(port)||Number(port)<1||Number(port)>65535){toast('面板端口必须是 1–65535',true);return}
 const changing=settingsCredentialChangedV6(f),current=changing?String(formValue(f,'currentPassword')||''):''
 if(changing&&!current){syncAdminReauthV6();f.elements.namedItem('currentPassword')?.focus();toast('修改管理员账号或密码时需要确认当前密码',true);return}
 const body={panelListen:joinPanelListenV6(xportPanelListenHostV6,port),panelBasePath:formValue(f,'panelBasePath'),panelDomain:formValue(f,'panelDomain'),panelCertFile:formValue(f,'panelCertFile'),panelKeyFile:formValue(f,'panelKeyFile'),xrayApiPort:Number(formValue(f,'xrayApiPort')),portMin:Number(formValue(f,'portMin')),portMax:Number(formValue(f,'portMax')),defaultRealitySni:formValue(f,'defaultRealitySni'),defaultRealityDest:formValue(f,'defaultRealityDest'),adminUsername:formValue(f,'adminUsername'),currentPassword:current,newPassword:formValue(f,'newPassword')}
 try{const d=await api('/api/settings',{method:'PUT',body:JSON.stringify(body)});await loadSettingsV6();toast(d.restartRequired?'设置已保存；重启 X-port 后生效':'设置已保存')}catch(x){toast(x.message,true)}
}

function initV6(){
 loadSettings=loadSettingsV6
 const f=$('#settings-form');if(f){f.onsubmit=saveSettingsV6;f.querySelector('.listen-hint')?.remove();f.elements.namedItem('adminUsername')?.addEventListener('input',syncAdminReauthV6);f.elements.namedItem('newPassword')?.addEventListener('input',syncAdminReauthV6)}
 const add=$('#add-account');if(add)add.onclick=()=>void openAccountModalV3()
}
document.addEventListener('DOMContentLoaded',initV6)
''')

          Path('internal/buildinfo/version.go').write_text('package buildinfo\n\nconst Current = "0.1.5"\n')

      - name: Format and validate source
        run: |
          set -euo pipefail
          gofmt -w internal/service/ports.go internal/service/port_suggestion.go internal/server/port_suggestion.go internal/server/ux_routes.go internal/accountcfg/account.go internal/service/expert.go
          node --check web/dist/v6.js
          go test ./...
          go vet ./...

      - name: Install browser runtime
        run: |
          npm init -y >/dev/null 2>&1
          npm install --no-save playwright@1.55.0 >/dev/null
          npx playwright install --with-deps chromium >/dev/null

      - name: Verify actual bundled frontend
        run: |
          cat > mock-v015.py <<'PY'
          import json,os
          from http.server import ThreadingHTTPServer,SimpleHTTPRequestHandler
          from pathlib import Path
          ROOT=Path(os.environ['GITHUB_WORKSPACE'])/'web'/'dist'
          A={"id":1,"name":"vless+XHTTP_test","enabled":True,"disabledReason":"","editable":True,"port":40836,"protocol":"vless","security":"none","network":"xhttp","upBytes":1,"downBytes":2,"allTimeBytes":3,"quotaBytes":0,"expiryTime":0,"monthlyReset":False,"credential":"d84ff80b-e4d2-45c5-9778-f298e39f040d","flow":"xtls-rprx-vision","host":"","path":"/vlessxhttptest","tag":"xport-40836-test"}
          E={"protocol":"vless","method":"xhttp","security":"none","acceptProxyProtocol":False,"supportsFallbacks":False,"supportsHttpHeader":False,"supportsReality":False,"supportsTls":False,"supportsXhttp":True,"xhttpMode":"auto","xhttpXPaddingBytes":"100-1000","xhttpNoSseHeader":False,"xhttpScMaxBufferedPosts":30,"xhttpScMaxEachPostBytes":"1000000","xhttpScStreamUpServerSecs":"20-80","xhttpUplinkHttpMethod":"POST"}
          class H(SimpleHTTPRequestHandler):
           def translate_path(self,path):
            q=path.split('?',1)[0];return str(ROOT/('index.html' if q=='/' else q.lstrip('/')))
           def j(self,o,status=200):
            b=json.dumps(o).encode();self.send_response(status);self.send_header('Content-Type','application/json');self.send_header('Content-Length',str(len(b)));self.end_headers();self.wfile.write(b)
           def do_GET(self):
            p=self.path.split('?',1)[0]
            if p=='/api/overview':return self.j({"system":{"hostname":"QiLin","os":"Linux","uptimeSeconds":100,"memoryUsed":1,"memoryTotal":2,"diskUsed":1,"diskTotal":2,"networkRx":1,"networkTx":1,"xrayActive":True,"xrayVersion":"26.3.27"},"accounts":{"total":1,"enabled":1},"xportVersion":"0.1.5"})
            if p=='/api/accounts':return self.j({"accounts":[A]})
            if p=='/api/accounts/1':return self.j(A)
            if p=='/api/accounts/online':return self.j({"peers":{"1":0},"connections":{"1":0}})
            if p=='/api/accounts/1/expert':return self.j(E)
            if p=='/api/accounts/port-suggestion':return self.j({"port":42317,"min":20000,"max":60000})
            if p=='/api/settings':return self.j({"panelListen":":50010","panelBasePath":"/YTjAodrPHeQ5TLI/","panelDomain":"","panelCertFile":"/root/cert/example/fullchain.pem","panelKeyFile":"/root/cert/example/privkey.pem","xrayApiPort":10085,"portMin":20000,"portMax":60000,"defaultRealitySni":"","defaultRealityDest":"","adminUsername":"admin-test"})
            if p=='/api/xray/update':return self.j({"current":"26.3.27","latest":"26.3.27","available":False})
            if p=='/api/xray/rollback':return self.j({"available":False})
            if p=='/api/health':return self.j({"ok":True,"database":True})
            if p=='/api/backups':return self.j({"backups":[]})
            if p=='/api/logs':return self.j({"logs":"mock"})
            return super().do_GET()
           def do_POST(self):return self.j({"id":2,"name":"new","port":42317},201)
           def do_PUT(self):return self.j(E)
           def do_PATCH(self):return self.j({"ok":True})
          ThreadingHTTPServer(('127.0.0.1',8878),H).serve_forever()
          PY
          python mock-v015.py & echo $! >mock-v015.pid
          cat > shot-v015.js <<'JS'
          const {chromium}=require('playwright');
          (async()=>{const b=await chromium.launch({headless:true});const p=await b.newPage({viewport:{width:1100,height:900}});await p.goto('http://127.0.0.1:8878/',{waitUntil:'networkidle'});await p.waitForTimeout(500);
          await p.click('button[data-page="accounts"]');await p.waitForTimeout(350);await p.click('.account-row button[data-act="edit"]');await p.waitForTimeout(300);
          const card=await p.locator('.modal-card').evaluate(e=>({cw:e.clientWidth,sw:e.scrollWidth}));if(card.sw>card.cw+1)throw new Error('horizontal overflow remains '+JSON.stringify(card));
          const footer=await p.locator('.account-editor-footer').boundingBox(),body=await p.locator('#modal-body').boundingBox();if(!footer||!body||footer.x<body.x-1||footer.x+footer.width>body.x+body.width+1)throw new Error('footer exceeds modal content bounds');
          const flowVisible=await p.locator('.vless-flow').isVisible().catch(()=>false);if(flowVisible)throw new Error('Vision Flow remains visible for XHTTP+NONE');
          await p.click('#account-advanced summary');await p.waitForTimeout(250);if(!await p.locator('#expert-xhttp-mode').count()||!await p.locator('#expert-xhttp-padding').count())throw new Error('existing XHTTP advanced fields missing');
          await p.screenshot({path:'edit-account-v015.png',fullPage:true});await p.click('#modal-close');await p.click('#add-account');await p.waitForTimeout(350);
          if(await p.locator('input[name="port"]').inputValue()!=='42317')throw new Error('suggested port not prefilled');const note=await p.locator('.account-port-note').textContent();if(!note.includes('20000')||!note.includes('60000'))throw new Error('port range hint wrong');
          await p.selectOption('#network-select','xhttp');await p.selectOption('#security-select','reality');await p.click('#account-advanced summary');await p.waitForTimeout(150);if(!await p.locator('#expert-xhttp-mode').count()||!await p.locator('#expert-xhttp-streamsecs').count())throw new Error('new XHTTP advanced fields missing');if(await p.locator('.vless-flow').isVisible())throw new Error('Vision visible on new XHTTP');
          await p.screenshot({path:'new-xhttp-v015.png',fullPage:true});await p.click('#modal-close');await p.click('button[data-page="system"]');await p.waitForTimeout(300);
          const pl=p.locator('#settings-form [name="panelListen"]'),bp=p.locator('#settings-form [name="panelBasePath"]');if(await pl.inputValue()!=='50010')throw new Error('panel port not normalized');if(await p.getByText('面板端口',{exact:true}).count()!==1)throw new Error('panel port label missing');if(await p.locator('#settings-form [name="currentPassword"]').count())throw new Error('permanent current password field remains');
          const a=await pl.boundingBox(),c=await bp.boundingBox();if(Math.abs(a.y-c.y)>2||Math.abs(a.height-c.height)>2)throw new Error('panel port/Base Path alignment mismatch');
          await p.locator('[name="newPassword"]').fill('new-password-123');if(await p.locator('#settings-current-password-wrap input[type="password"]').count()!==1)throw new Error('conditional reauth field missing');await p.locator('[name="newPassword"]').fill('');if(await p.locator('#settings-current-password-wrap').count())throw new Error('reauth field did not disappear');
          await p.screenshot({path:'system-v015.png',fullPage:true});await b.close()})().catch(e=>{console.error(e);process.exit(1)})
          JS
          node shot-v015.js
          kill $(cat mock-v015.pid) || true

      - uses: actions/upload-artifact@v4
        with:
          name: v015-real-ui-screenshots
          path: |
            edit-account-v015.png
            new-xhttp-v015.png
            system-v015.png

      - name: Commit verified source changes
        run: |
          rm -f package.json package-lock.json mock-v015.py mock-v015.pid shot-v015.js
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add internal web/dist
          git commit -m "fix: align inbound editor and automatic ports"
          git push origin HEAD:fix/v0.1.5-inbound-ui
